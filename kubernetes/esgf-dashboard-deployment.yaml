apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose -f /Users/greguska/githubprojects/esgf/esgf-docker/docker-compose-greguska.yml
      convert
    kompose.version: 1.7.0 (767ab4b)
  creationTimestamp: null
  labels:
    io.kompose.service: esgf-dashboard
  name: esgf-dashboard
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: esgf-dashboard
    spec:
      containers:
      - image: esgfhub/esgf-dashboard:1.4
        name: dashboard
        ports:
        - containerPort: 8080
        - containerPort: 8443
        resources: {}
        volumeMounts:
        - mountPath: /etc/certs/
          name: esgf-dashboard-claim0
        - mountPath: /etc/httpd/conf.d/esgf-httpd.conf
          name: esgf-dashboard-claim1
        - mountPath: /root/archives/grid_security_certs.tar.xz
          name: esgf-dashboard-claim2
        - mountPath: /root/archives/esgf_config.tar.xz
          name: esgf-dashboard-claim3
      restartPolicy: Always
      volumes:
      - name: esgf-dashboard-claim0
        persistentVolumeClaim:
          claimName: esgf-dashboard-claim0
      - name: esgf-dashboard-claim1
        persistentVolumeClaim:
          claimName: esgf-dashboard-claim1
      - name: esgf-dashboard-claim2
        persistentVolumeClaim:
          claimName: esgf-dashboard-claim2
      - name: esgf-dashboard-claim3
        persistentVolumeClaim:
          claimName: esgf-dashboard-claim3
status: {}
